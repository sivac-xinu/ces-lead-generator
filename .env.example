# CES Lead Generator — Environment Configuration
# Copy this file to .env and fill in your values.

# Authentication: set a password to restrict access to the app.
# Leave empty/unset to disable authentication (no login required).
APP_PASSWORD=your_secure_password_here

# Path to the call log JSON file. Defaults to ./call_log.json if not set.
CALL_LOG_PATH=
